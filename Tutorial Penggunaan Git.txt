Penggunaan Github

Upload Pertama

1. git init
2. git remote set-url origin https://github.com/MyRepo/project.git
3. git add .
4. git commit -m "force push"
3. git pull origin master
5. git push origin master --force

Upload lainnya

1. git init
2. git remote add origin https://github.com/MyRepo/project.git
3. git pull origin master
4. git push origin master --force
